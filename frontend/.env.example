# Backend API URL — set when the frontend runs on a different machine than the server.
# Leave empty for same-origin (built UI served by FastAPI, or Vite dev proxy).
# VITE_API_BASE_URL=http://192.168.1.100:8000
